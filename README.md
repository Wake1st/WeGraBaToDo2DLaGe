# Weighted Tree Graph Based 2D Land Generation

This is an experiment - multiple systems will need to be build and run in stages (separate modules provide a nicer plugin ecosystem which bevy relies on).